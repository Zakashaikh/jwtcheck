% Generated by benchmark/make_figures.py -- do not edit by hand.
\begin{figure}[htbp]
\centering
\begin{lstlisting}[basicstyle=\ttfamily\scriptsize,frame=single,breaklines=true,columns=fullflexible]
$ python benchmark/portswigger_eval.py

============================================================
RQ3 — PortSwigger JWT lab classes
============================================================
  [n/a ] 1. Auth bypass via unverified signature
  [PASS] 2. Auth bypass via flawed signature verification (none)
  [PASS] 3. Auth bypass via weak signing key
  [PASS] 4. JWK header injection
  [PASS] 5. JKU header injection
  [PASS] 6. kid header path traversal
  [n/a ] 7. Algorithm confusion (RS->HS)
  [PASS] 8. kid header SQL injection
------------------------------------------------------------
Overall surfaced       : 6/8
Token-detectable hits  : 6/6

Written: benchmark/portswigger_results.md / .json
\end{lstlisting}
\caption{Token-analyser evaluation against the eight PortSwigger JWT laboratory attack classes. Six of eight classes are surfaced; the two marked \texttt{n/a} are server-side behaviours not decidable from a token alone.}
\label{fig:portswigger}
\end{figure}
